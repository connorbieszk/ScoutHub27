{
  "name": "scouthub27",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm --recursive build",
    "web": "pnpm --filter web",
    "api": "pnpm --filter api"
  }
}
